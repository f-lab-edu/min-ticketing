<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} [%level] [%thread] [%logger{36}] - %msg%n
            </pattern>
        </encoder>
    </appender>

    <appender name="AMQP" class="org.springframework.amqp.rabbit.logback.AmqpAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <timestampPattern>yyyy-MM-dd HH:mm:ss.SSS</timestampPattern>
            <includeMdc>false</includeMdc>
            <includeCallerData>false</includeCallerData>
            <fieldNames>
                <timestamp>timestamp</timestamp>
                <level>level</level>
                <message>message</message>
            </fieldNames>
        </encoder>
        <host>${MQ_HOST}</host>
        <port>${MQ_PORT}</port>
        <username>${MQ_USERNAME}</username>
        <password>${MQ_PASSWORD}</password>
        <exchangeName>${MQ_EXCHANGENAME}</exchangeName>
        <routingKeyPattern>${MQ_ROUTING_KEY_NAME}</routingKeyPattern>
        <charset>UTF-8</charset>
        <deliveryMode>PERSISTENT</deliveryMode>
        <contentType>application/json</contentType>
        <declareExchange>false</declareExchange>
        <addMdcAsHeaders>false</addMdcAsHeaders>
    </appender>


    <springProfile name="dev">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="prod">
        <logger name="CONSOLE" level="WARN" additivity="false">
            <appender-ref ref="CONSOLE"/>
        </logger>
        <root level="INFO">
            <appender-ref ref="AMQP"/>
        </root>
    </springProfile>
</configuration>